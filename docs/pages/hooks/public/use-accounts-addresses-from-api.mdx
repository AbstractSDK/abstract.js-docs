import ParameterQuerySnippet from '../parameter-query-snippet.mdx'
import ParameterExtraSnippet from './parameter-extra-snippet.mdx'
import ParameterCommonSnippet from './parameter-common-snippet.mdx'

# useAccountsAddressesFromApi [Hook for fetching addresses of multiple Abstract accounts]

## Import

```ts twoslash
import { useAccountsAddressesFromApi } from '@abstract-money/react'
```

## Usage

```tsx twoslash [index.tsx]
import { useAccountsAddressesFromApi } from '@abstract-money/react'
import { stringToAccountId } from '@abstract-money/core'

function App() {
  const accountsBaseAddresses = useAccountsAddressesFromApi({
    chainName: 'pion',
    args: {
      accountIds: [
        stringToAccountId('pion-1'),
        stringToAccountId('pion-2'),
        stringToAccountId('pion-3'),
      ],
    },
  })
}
```

## Parameters

<ParameterCommonSnippet />

### args

Arguments passed to the hook

#### accountIds
`AccountId[]` Array of account IDs to fetch base addresses for.

<ParameterExtraSnippet />
<ParameterQuerySnippet />
